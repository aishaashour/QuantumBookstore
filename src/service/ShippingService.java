package service;

public class ShippingService {
    //no implementation required just to make sure everything is ok
    public static void send(String address) {
        System.out.println("ShippingService called for address: " + address);
    }
}
